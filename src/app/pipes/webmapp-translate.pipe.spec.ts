import { WebmappTranslatePipe } from './webmapp-translate.pipe';

describe('WebmappTranslatePipe', () => {
  it('create an instance', () => {
    const pipe = new WebmappTranslatePipe();
    expect(pipe).toBeTruthy();
  });
});
